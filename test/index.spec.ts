import lib from '../src'

// ----------------------------------------------------------------------------

describe('module_name', () => {

  it('should do a thing', () => {
    expect(lib).to.exist
  })

})
